// The brand style spec for generated icons, extracted from the seed set.
// Prepended to every icon generation so output stays on-brand.
export const ICON_STYLE_SPEC = `Simify icons are clean line-art vectors with this exact style:
- viewBox="0 0 48 48"
- fill="none"
- stroke="#0E7C66" (Simify brand teal)
- stroke-width="2.2"
- stroke-linecap="round"
- stroke-linejoin="round"
- Simple, friendly, single-weight line construction on a 48px grid
- Generous padding; no text, no filled shapes, no gradients, no drop shadows`;
